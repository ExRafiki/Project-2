<div class="white-text">
<%# //----------ERROR BANNER------------------------------------------------- %>
<div class="row">
  <% if(messages.danger){ %>
    <div class="col s12 l12">
      <div class="card red lighten-3">
        <div class="card-content red-text">
          <p class="card-title"><%= messages.danger %></p>
        </div>
      </div>
    </div>
  <% } %>
<%# //----------------------------------------------------------------------- %>
  <h2 id="Logo">Account</h2>
  <form class="col l12" action="/users/<%= user.id%>" method="post">
    <input type="hidden" name="_method" value="PUT">
      <div class="input-field col l12">
      <input type="text" name="username" id="username_field" value="<%= locals.user.username %>">
      <label for="username_field">Username</label>
    </div>
    <div class="input-field col l12">
      <input type="email" name="email" id="email_field" value="<%= locals.user.email %>">
      <label for="email_field">Email</label>
    </div>
    <div class="input-field col l12">
      <button class="green btn"> Finish Edit</button>
    </div>
  </form>
</div>
</div>
